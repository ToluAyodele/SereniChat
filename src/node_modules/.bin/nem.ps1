#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/ruby$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/ruby$exe"  "$basedir/../nem/bin/nem" $args
  } else {
    & "$basedir/ruby$exe"  "$basedir/../nem/bin/nem" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "ruby$exe"  "$basedir/../nem/bin/nem" $args
  } else {
    & "ruby$exe"  "$basedir/../nem/bin/nem" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
